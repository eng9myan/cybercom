from django.apps import AppConfig


class SisConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "products.cyed.sis"
    label = "cyed_sis"
    verbose_name = "CyEd — Student Information System"
