from django.apps import AppConfig


class ReportingConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "products.cyed.reporting"
    label = "cyed_reporting"
    verbose_name = "CyEd — Reporting"
