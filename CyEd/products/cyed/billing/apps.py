from django.apps import AppConfig


class BillingConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "products.cyed.billing"
    label = "cyed_billing"
    verbose_name = "CyEd — Billing & Installments"
