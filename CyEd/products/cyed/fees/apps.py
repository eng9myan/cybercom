from django.apps import AppConfig


class FeesConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "products.cyed.fees"
    label = "cyed_fees"
    verbose_name = "CyEd — Fees & Billing"
