from django.apps import AppConfig


class EventsConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "products.cyed.events"
    label = "cyed_events"
    verbose_name = "CyEd — Events & Excursions"
