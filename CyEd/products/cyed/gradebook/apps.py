from django.apps import AppConfig


class GradebookConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "products.cyed.gradebook"
    label = "cyed_gradebook"
    verbose_name = "CyEd — Gradebook"
