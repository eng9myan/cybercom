from django.apps import AppConfig


class PayrollConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "products.cyed.payroll"
    label = "cyed_payroll"
    verbose_name = "CyEd — Payroll"
