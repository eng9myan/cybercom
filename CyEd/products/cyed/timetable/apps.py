from django.apps import AppConfig


class TimetableConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "products.cyed.timetable"
    label = "cyed_timetable"
    verbose_name = "CyEd — Timetable"
