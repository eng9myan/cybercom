from django.apps import AppConfig

class RegistriesConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "products.cymed.core.registries"
    label = "cymed_registries"
