from django.apps import AppConfig

class labaccessioningConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "products.cymed.laboratory.accessioning"
    label = "lab_accessioning"
    verbose_name = "CyMed Laboratory — Accessioning"

