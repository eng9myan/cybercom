"""Django app config for the CyMed ecosystem shared capacity sub-app."""
from django.apps import AppConfig


class SharedCapacityConfig(AppConfig):
    name = "products.cymed.ecosystem.shared_capacity"
    label = "cymed_eco_shared_capacity"
    verbose_name = "CyMed Ecosystem — Shared Inventory / Capacity / Pool"
