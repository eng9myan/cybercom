"""App config for CyMed ecosystem provider_directory sub-app."""
from django.apps import AppConfig


class ProviderDirectoryConfig(AppConfig):
    name = "products.cymed.ecosystem.provider_directory"
    label = "cymed_eco_provider_directory"
    verbose_name = "CyMed Ecosystem — Provider Network Directory"
