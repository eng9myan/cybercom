pytest>=7.0
pytest-django>=4.5
pytest-mock>=3.10

# scripts/build_ar_locale.py — compiles locale/*/LC_MESSAGES/django.{po,mo}
# without GNU gettext (msgfmt), which the CI/deploy image doesn't ship.
polib>=1.2.0
