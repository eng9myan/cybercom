__pycache__/
*.pyc
*.pyo
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
node_modules/
.next/
.turbo/
dist/
build/
*.sqlite3
.env
.env.local
.venv/
venv/
coverage.xml
htmlcov/
cycom/media/

# Ruflo local secrets and runtime data
.env.*.local
.claude-flow/data/
.claude-flow/logs/
.claude-flow/sessions/

# Session dev artifacts (never commit — logs may contain printed secrets)
cycom/dev_backend_*.log
cycom/cycom-erp/dev_frontend_*.log
infrastructure/dev-kc8085.override.yml
infrastructure/.env.keycloak

# Prod-box deploy secrets (generated on the host by scripts/deploy_box.sh)
infrastructure/.env
infrastructure/.env.production
infrastructure/.env.frontend
.env.production
.env.frontend
infrastructure/.env.demo

# ── Claude Code / ruflo local state (regenerable via `ruflo init`) ──
.claude/
.claude-flow/
.swarm/
.agents/

# Local dev databases / smoke fixtures
*.db
dev_smoke.db

# Local dev Keycloak realm — contains a dev seed-user password
infrastructure/keycloak/cycom-dev-realm.json

# Education product — pending scope decision (blueprint J.2 O5)
/CyEd/
cycom/.coverage

# Operational-simulation KPI exports (regenerated by seed_*_sim commands)
simulation_output/
cymed/.venv/
