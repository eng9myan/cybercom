<?xml version="1.0" encoding="utf-8"?>
<cycom>
    <data>
        <!-- Action available from account.move list views ("Action" menu) -->
        <record id="action_account_move_bulk_set_to_draft" model="ir.actions.server">
            <field name="name">Set to Draft</field>
            <field name="model_id" ref="account.model_account_move"/>
            <field name="binding_model_id" ref="account.model_account_move"/>
            <field name="binding_view_types">list</field>
            <field name="state">code</field>
            <field name="group_ids" eval="[(4, ref('account.group_account_manager'))]"/>
            <field name="code">
if records:
    records.filtered(lambda m: m.state != 'draft').button_draft()
            </field>
        </record>
    </data>
</cycom>

