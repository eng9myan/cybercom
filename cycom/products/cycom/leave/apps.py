from django.apps import AppConfig


class LeaveConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "products.cycom.leave"
    label = "cycom_leave"
    verbose_name = "Cycom — Leave Management"
