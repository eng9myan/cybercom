from django.apps import AppConfig


class ExpensesConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "products.cycom.expenses"
    label = "cycom_expenses"
    verbose_name = "Cycom — Expenses"
