from django.apps import AppConfig


class FleetConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "products.cycom.fleet"
    label = "cycom_fleet"
    verbose_name = "Cycom — Fleet"
