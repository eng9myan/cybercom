from django.apps import AppConfig


class SecurityConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "platform.security"
    label = "platform_security"
    verbose_name = "Platform Security"
